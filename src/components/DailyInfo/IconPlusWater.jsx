import Icon from '../shared/Icon/Icon.jsx';

export const IconPlusWater = ({ className, iconId, width, height }) => {
  return (
    <Icon
      className={`${className}`}
      id={iconId}
      width={width}
      height={height}
    />
  );
};
