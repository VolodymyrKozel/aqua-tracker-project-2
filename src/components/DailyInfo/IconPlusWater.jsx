import Icon from '../shared/Icon/Icon.jsx';

export const IconPlusWater = ({ className }) => {
  return (
    <Icon
      className={`${className}`}
      id="icon-plus-in-circle"
      width="30"
      height="30"
    />
  );
};
