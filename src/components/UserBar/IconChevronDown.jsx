import Icon from '../shared/Icon/Icon.jsx';

export const IconChevronDown = ({ className }) => {
  return (
    <Icon
      className={`${className}`}
      id="icon-ChevronDown"
      width="16"
      height="16"
    />
  );
};
