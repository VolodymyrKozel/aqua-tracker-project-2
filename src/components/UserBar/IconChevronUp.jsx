import Icon from '../shared/Icon/Icon.jsx';

export const IconChevronUp = ({ className }) => {
  return (
    <Icon
      className={`${className}`}
      id="icon-chevron-up"
      width="16"
      height="16"
    />
  );
};
