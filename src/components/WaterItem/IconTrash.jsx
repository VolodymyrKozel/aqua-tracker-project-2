import Icon from '../shared/Icon/Icon.jsx';

const IconTrash = ({ className }) => {
  return (
    <Icon className={`${className}`} id="icon-trash" width="14" height="14" />
  );
};

export default IconTrash;
