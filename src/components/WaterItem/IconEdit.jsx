import Icon from '../shared/Icon/Icon.jsx';

export const IconEdit = ({ className }) => {
  return (
    <Icon className={`${className}`} id="icon-edit" width="14" height="14" />
  );
};

export default IconEdit;
