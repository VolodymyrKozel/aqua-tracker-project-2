import Icon from '../shared/Icon/Icon.jsx';

export const IconGlass = ({ className }) => {
  return (
    <Icon
      className={`${className}`}
      id="icon-glass-fill"
      width="38"
      height="38"
    />
  );
};

export default IconGlass;
